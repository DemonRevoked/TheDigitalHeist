# NET-02 — DoH Rhythm (Metadata Exfil)

This challenge is **offline**. Players download the PCAP from `challenge-files/net-02/` and recover the flag.

Author notes:
- Generated by `src/generate_pcap.py` (pure Python, no external deps).
- Intentionally **not decryptable**; the solve uses a side-channel from TLS-like record lengths + packet timing.


