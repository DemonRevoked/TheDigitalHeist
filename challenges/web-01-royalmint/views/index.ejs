<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <title>CTF — IDOR</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <div class="container">
    <h1>Leaky Profile (Easy)</h1>
    <p>This portal lets you view invoices.</p>

    <% if (!user) { %>
      <a class="btn" href="/auth/login">Login</a>
    <% } else { %>
      <p>Logged in as <b><%= user.username %></b></p>
      <a class="btn" href="/me">My profile</a>
      <form method="post" action="/auth/logout" style="display:inline">
        <button class="btn secondary" type="submit">Logout</button>
      </form>
    <% } %>

    <hr />
    <p class="muted">For training use only. Target only this instance.</p>
  </div>
</body>
</html>
